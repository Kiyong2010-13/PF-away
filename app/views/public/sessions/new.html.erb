<div class="container">
  <div class="row">
    <div class="col-sm-4"></div>
    <div class="col-sm-4 my-5">
      <h2 class="mb-4 text-center">ログイン</h2>
      <%= form_with model: resource, url: customer_session_path, local: true do |f| %>
        <div class="field">
          <%= f.label :メールアドレス %><br />
          <%= f.email_field :email, class: "form-control mb-2", autofocus: true, autocomplete: "email" %>
        </div>

        <div class="field">
          <%= f.label :パスワード %><br />
          <%= f.password_field :password, class: "form-control mb-4" , autocomplete: "current-password" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="field mb-2 mb-3">
            <%= f.check_box :remember_me %>
            <%= f.label :ログイン情報を保存する %>
          </div>
        <% end %>

        <div class="actions">
          <%= f.submit "ログイン", class: "form-control btn btn-info" %>
        </div>
      <% end %>
      <h5 class="mt-5">※会員登録がお済みでない方</h5>
      <%= link_to "こちら", new_customer_registration_path %>から新規登録を行ってください。
    </div>
    <div class="col-sm-4"></div>
  </div>
</div>
